// (uses key in first column, then selects views based on the previous key in the left column to the next space on the right column. 
// very weird when comparing to other tables. the randomness is also very... not random. im not sure how that works tbh)
add_column|Key|hash_string
add_column|LoadingTextRefId|hash_string
